<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zb.mapper.UserMapper">
    <select id="getList" resultType="com.zb.entity.TbUser">

        SELECT 	username,
            PASSWORD,
            phone,
            email,
            created,
            updated,
            source_type,
            nick_name,
            NAME,
            STATUS,
            head_pic,
            qq,
            is_mobile_check,
            is_email_check,
            sex,
            user_level,
            points,
            experience_value,
            birthday,
            last_login_time

            FROM
            shop_user.tb_user

    </select>

</mapper>